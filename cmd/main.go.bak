package main

import (
	"athgene/twentythreeandme"
	"net/http"
	"os"
)

func main() {
	http.HandleFunc("/", twentythreeandme.HandleDownload)
	http.ListenAndServe(port(), nil)
}

func port() string {
	port := os.Getenv("PORT")
	if len(port) == 0 {
		port = "8080"
	}
	return ":" + port
}
